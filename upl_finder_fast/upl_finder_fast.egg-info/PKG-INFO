Metadata-Version: 2.4
Name: upl_finder_fast
Version: 0.1.0
Summary: Fast qPCR primer + Roche UPL probe design (Rust-accelerated)
Requires-Python: >=3.10
Description-Content-Type: text/markdown
Requires-Dist: streamlit>=1.32
Requires-Dist: requests>=2.31
Requires-Dist: pandas>=2.0
Requires-Dist: primer3-py>=2.0.3
Requires-Dist: pydantic>=2.5

# upl_finder_fast

高速版のCLIパッケージです（Rust拡張が利用可能な場合は自動で使用します）。

## Conda 環境 / Conda Environment

```bash
conda env create -f ../environment_fast.yml
conda activate upl_finder_fast
```

Conda environment setup (English):

```bash
conda env create -f ../environment_fast.yml
conda activate upl_finder_fast
```

## 実行 / Run

```bash
upl_finder_fast --param ../parameter.toml --gene ERBB4
```

Run (English):

```bash
upl_finder_fast --param ../parameter.toml --gene ERBB4
```
